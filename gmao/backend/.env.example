# xmaint Backend Configuration (maintx.org)
NODE_ENV=development
PORT=5000

# JWT Secret - Générer une clé sécurisée en production
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# Limite de tentatives de connexion par IP (par fenêtre de 15 min). En dev, augmenter si 429.
# LOGIN_RATE_MAX=100

# Base centrale admin MAINTX (tenants, abonnements, utilisateurs). Pas de données GMAO opérationnelles.
GMAO_DB_PATH=gmao.db
# Base client par défaut pour démo / connexion sans tenant (sites, équipements, OT).
# GMAO_DEFAULT_CLIENT_DB=default.db

# Notifications Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=maintx <noreply@maintx.org>

# Notifications SMS (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=+33XXXXXXXXX

# Assistance IA dans les OT (optionnel) — une seule clé suffit ; priorité : Groq > Gemini > OpenAI
# --- Options GRATUITES ---
# Groq (gratuit, rapide) : https://console.groq.com → clé API
# GROQ_API_KEY=gsk_...
# GROQ_MODEL=llama-3.3-70b-versatile
# Google Gemini (gratuit) : https://aistudio.google.com/apikey
# GEMINI_API_KEY=...
# GEMINI_MODEL=gemini-2.0-flash
# --- Option payante ---
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
